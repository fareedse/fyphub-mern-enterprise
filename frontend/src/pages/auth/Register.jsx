import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import toast from 'react-hot-toast';
import { useAuth } from '../../context/AuthContext';
import { Field } from '../../components/UI';
import { getError } from '../../api/client';
export default function Register(){const [f,setF]=useState({name:'',email:'',phone:'',password:'',confirm:''}),[loading,setLoading]=useState(false);const {register}=useAuth();const nav=useNavigate();const submit=async(e)=>{e.preventDefault();if(f.password!==f.confirm)return toast.error('Passwords do not match');setLoading(true);try{await register(f);toast.success('Account created');nav('/dashboard')}catch(err){toast.error(getError(err))}finally{setLoading(false)}};return <div className="auth-wrap"><form className="auth-card" onSubmit={submit}><h1>Create Account</h1><Field label="Name" required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><Field label="Email" type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><Field label="WhatsApp" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/><Field label="Password" type="password" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><Field label="Confirm Password" type="password" required value={f.confirm} onChange={e=>setF({...f,confirm:e.target.value})}/><p><input required type="checkbox"/> I accept the terms.</p><button className="btn primary" style={{width:'100%'}} disabled={loading}>Create Account</button><p><Link to="/login">Already have an account?</Link></p></form></div>}
